exports.generateTopicByAI = async (keyword) => {
  return `🔥 AI đề xuất chủ đề: "Top 5 mẹo học lập trình hiệu quả trong 2025"\n=> Dựa trên từ khóa: "${keyword || 'ngẫu nhiên'}"`;
};
exports.generateScriptByAI = async (keyword) => {
  return `🔥 AI đề xuất kịch bản: "Top 5 mẹo học lập trình hiệu quả trong 2025"\n=> Dựa trên từ khóa: "${keyword || 'ngẫu nhiên'}"`;
};