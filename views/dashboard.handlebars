<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Dashboard - AI Short Video Creator</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap + Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        :root {
            --main-bg: #1c1b1b;
            --card-bg: linear-gradient(to bottom, gray, black);
            --accent: #FFC107;
            --accent-dark: #e6b213;
            --text-light: #fff;
            --text-muted: #F1CA4B;
            --footer-bg: #2A2F3A;
        }

        body {
            background: linear-gradient(to bottom, #383636, #302f2f);
            /* từ xám nhạt đến đen */
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            padding-bottom: 60px;
        }


        main {
            flex: 1 0 auto;
        }

        .title-glow {
            color: var(--accent);
        }

        .text-muted {
            color: var(--text-muted) !important;
        }

        .card {
            background-color: var(--card-bg);
            color: var(--text-light);
            transition: transform 0.2s ease, box-shadow 0.2s;
        }

        .card:hover {
            transform: scale(1.02);
            box-shadow: 0 4px 12px gray;
        }

        .card-title {
            font-weight: 600;
            color: var(--accent);
        }

        .card-img-top {
            width: 100%;
            max-height: 300px;
            aspect-ratio: 1024 / 1526;
            object-fit: contain;
            background: linear-gradient(to bottom, #3b3937, #1a2b2d);
            /* từ xám sang xanh đen */
            border-radius: 0.375rem 0.375rem 0 0;
        }


        .btn-outline-primary {
            color: var(--accent);
            border-color: var(--accent);
        }

        .btn-outline-primary:hover {
            background-color: var(--accent-dark);
            color: var(--text-light);
            border-color: var(--accent-dark);
        }

        footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            background-color: var(--footer-bg);
            padding: 15px 0;
            text-align: center;
            color: var(--text-light);
        }
    </style>
</head>

<body>
    <main class="container mt-5">
        <div class="text-center mb-5">
            <h2 class="fw-bold title-glow">Bảng điều khiển - AI Short Video Creator</h2>
            <p class="text-muted">Chọn một chức năng để bắt đầu sáng tạo video ngắn với AI</p>
        </div>

        <div class="row g-4">
            <!-- Gợi ý xu hướng -->
            <div class="col-md-6">
                <div class="card h-100 shadow-sm">
                    <img src="/image/image1.png" class="card-img-top" alt="Gợi ý xu hướng">
                    <div class="card-body text-center">
                        <h5 class="card-title">🎯 Gợi ý chủ đề theo xu hướng</h5>
                        <p class="card-text">Khám phá những ý tưởng hot nhất hiện nay thông qua AI và API trend.</p>
                        <a href="/search" class="btn btn-outline-primary">Bắt đầu</a>
                    </div>
                </div>
            </div>

            <!-- Quản lý video -->
            <div class="col-md-6">
                <div class="card h-100 shadow-sm">
                    <img src="/image/image2.png" class="card-img-top" alt="Quản lý video">
                    <div class="card-body text-center">
                        <h5 class="card-title">📁 Quản lý video đã tạo</h5>
                        <p class="card-text">Xem lại, chỉnh sửa hoặc tải lại những video bạn đã tạo trước đó.</p>
                        <a href="/my-videos" class="btn btn-outline-primary">Xem video</a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer>
        © 2025 AI ShortVideo. Made with ❤️ by Quang.
    </footer>
</body>

</html>