{{!-- views/videoView/myVideos.handlebars --}}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

<div class="container my-5">
  <h2 class="mb-4 text-center text-warning">📁 Video của bạn</h2>

  {{#if videos.length}}
  <div class="table-responsive">
    <table class="table table-dark table-striped align-middle">
      <thead>
        <tr>
          <th>#</th>
          <th>Tiêu đề</th>
          <th>Dung lượng (MB)</th>
          <th>Xem</th>
          <th>Up YouTube</th>
          <th>Xoá</th>
        </tr>
      </thead>
      <tbody>
        {{#each videos}}
        <tr id="row-{{id}}">
          <td>{{id}}</td>
          <td>{{title}}</td>
          <td>{{size_mb}}</td>
          <td>
            <a class="btn btn-sm btn-outline-info" href="{{public_url}}" target="_blank" title="Xem video">
              <i class="bi bi-play-circle"></i> Xem
            </a>
          </td>
          <td>
            <button class="btn btn-sm btn-outline-danger btn-youtube" title="Up video này lên YouTube" data-id="{{id}}"
              data-url="{{local_path}}" data-title="{{title}}" >
              <i class="bi bi-youtube"></i> YouTube
            </button>
          </td>
          <td>
            <button class="btn btn-sm btn-outline-danger btn-del" title="Xoá video này" data-id="{{id}}">
              <i class="bi bi-trash"></i> Xoá
            </button>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{else}}
  <p class="text-center text-muted">Chưa có video nào.</p>
  {{/if}}
</div>

<script>
  document.querySelectorAll('.btn-del').forEach(btn => {
    btn.addEventListener('click', async () => {
      const id = btn.dataset.id;
      if (!confirm('Xoá video #' + id + ' ?')) return;
      try {
        const res = await fetch('/api/videos/' + id, { method: 'DELETE' });
        const js = await res.json();
        if (js.success) {
          document.getElementById('row-' + id)?.remove();
        } else {
          alert('Lỗi: ' + (js.error || 'không rõ'));
        }
      } catch (e) {
        console.error(e);
        alert('❌ Xoá thất bại! Xem console để biết thêm chi tiết.');
      }
    });
  });

  document.querySelectorAll('.btn-youtube').forEach(btn => {
      btn.addEventListener('click', async () => {
        const id = btn.dataset.id;
        const url = btn.dataset.url;
        const title = btn.dataset.title;

        console.log('Up video #' + id + ' với URL:', url);
        if (!url) {
          alert('❌ Thiếu URL video.');
          return;
        }

        if (!confirm('Bạn muốn up video #' + id + ' lên YouTube?')) return;

        btn.disabled = true;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span> Đang up...';

        try {
          const res = await fetch('/api/videos/' + id + '/upload-youtube', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'include',
            body: JSON.stringify({ url,title })
          });

          const contentType = res.headers.get("content-type") || '';
          const text = await res.text();
          let js;

          if (!res.ok) {
            // Nếu là lỗi 401 → không đăng nhập
            if (res.status === 401) {
              alert('❌ Bạn chưa đăng nhập. Vui lòng đăng nhập lại.');
              window.location.href = '/login';
              return;
            }

            // Nếu là HTML (như trang login), không phải JSON
            if (!contentType.includes("application/json")) {
              console.error('❌ Server trả về HTML thay vì JSON:', text);
              alert('Lỗi: Server trả về không hợp lệ (HTML)');
              return;
            }
          }

          // Thử parse JSON
          try {
            js = JSON.parse(text);
          } catch (err) {
            console.error('❌ Không phải JSON:', text);
            alert('Lỗi: Server trả về không hợp lệ:\n' + text);
            return;
          }

          // Xử lý kết quả từ server
          if (js.success) {
            alert('✅ Đã up lên YouTube!\n👉 ' + js.youtubeUrl);
          } else {
            alert('❌ Upload thất bại: ' + (js.error || 'Không rõ lỗi'));
          }

        } catch (e) {
          console.error('❌ Lỗi khi gọi API upload:', e);
          alert('❌ Upload thất bại. Vui lòng kiểm tra console hoặc thử lại sau.');
        }

        btn.disabled = false;
        btn.innerHTML = '<i class="bi bi-youtube"></i> YouTube';
      });
  });
</script>