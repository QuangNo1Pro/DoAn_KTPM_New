<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Short Video Platform - Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
        integrity="sha512-pw1yR6gpE6fF7ZK+6rUv7uK6P+PscLE0dO5kYlLrR7vX+0o7GkY+e5r+K8uB93FqXkfwkF2gI5B1lS4oK/2xUg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            background-color: #0F1419;
            min-height: 100vh;
            position: relative;
            font-family: 'Segoe UI', sans-serif;
            margin: 0; /* Remove default margin to ensure full coverage */
        }

        #bg-video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%; /* Match the viewport width */
            height: auto; /* Maintain aspect ratio */
            object-fit: contain; /* Ensure full video is visible without cropping */
            z-index: -1; /* Place video behind all content */
            opacity: 0.5; /* Adjust for readability */
            pointer-events: none; /* Prevent interaction with video */
        }

        .vh-100 {
            position: relative; /* Ensure section content stays above video */
            z-index: 1; /* Place content above video */
        }

        .password-toggle-icon {
            padding: 0 10px; /* Adjust padding inside the icon container */
            background-color: #e9ecef; /* Match the input background */
            border: 1px solid #ced4da; /* Match the input border */
        }

        .input-group .form-control {
            flex: 1; /* Ensure input takes available space */
        }

        .input-group-text {
            padding: 0.375rem 0.75rem; /* Match Bootstrap input padding */
        }

        .login-label {
            color: #1D9BF0; /* Blue color matching the design */
            font-weight: bold;
            font-size: 2rem; /* Increased for prominence */
            text-align: center;
            margin-bottom: 20px; /* Space below the label */
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3); /* Shadow for depth */
            animation: pulse 1.5s infinite ease-in-out; /* Subtle pulsing effect */
        }

        /* Animation for pulsing effect */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        /* Enhance label visibility */
        .form-label {
            color: #FFFFFF; /* Bright white for contrast */
            font-size: 1.2rem; /* Larger size */
            font-weight: bold; /* Bold for emphasis */
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.5); /* Subtle shadow for readability */
        }

        /* Enhance registration text and link */
        .text-center.mt-3 p {
            color: #FFFFFF; /* Bright white */
            font-size: 1rem; /* Slightly larger */
            font-weight: bold;
        }

        .text-center.mt-3 a {
            color: #1D9BF0; /* Blue for link */
            font-weight: bold;
            text-decoration: underline; /* Underline for clarity */
        }

        .text-center.mt-3 a:hover {
            color: #06B6D4; /* Lighter blue on hover */
        }

        footer {
            background-color: #000; /* Dark background */
            color: #FBBF24; /* Yellowish color for contrast */
            padding: 15px 0;
            width: 100%;
            z-index: 2; /* Ensure it’s above the video */
        }

        /* Rounder corners for input fields */
        .form-control {
            border-radius: 10px; /* Increased from default 0.25rem (4px) */
        }

        .input-group-text {
            border-radius: 0 10px 10px 0; /* Match the input's right corners, keep left flat */
        }
    </style>
</head>

<body>
    <!-- Video background -->
    <video autoplay muted loop id="bg-video">
        <source src="/video_background/lofi.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <div class="d-flex flex-column min-vh-100">
        <section class="vh-100 flex-grow-1">
            <div class="container py-5 h-100">
                <div class="row d-flex justify-content-center align-items-center h-100">
                    <div class="col-12 col-md-9 col-lg-7 col-xl-6">
                        {{#if message}}
                        <div class="alert alert-danger" role="alert">
                            {{message}}
                        </div>
                        {{/if}}

                        <h2 class="login-label">Create an account</h2>

                        <form action="/register" method="POST">
                            <div data-mdb-input-init class="form-outline mb-4">
                                <label class="form-label" for="name">Your Name</label>
                                <input type="text" id="name" class="form-control form-control-lg" name="tennguoidung"
                                    placeholder="VD: Nguyen Van A" required />
                            </div>

                            <div data-mdb-input-init class="form-outline mb-4">
                                <label class="form-label" for="email">Your Email</label>
                                <input type="email" id="email" class="form-control form-control-lg" name="email"
                                    placeholder="VD: Tendangnhap@gmail.com" required />
                            </div>

                            <div data-mdb-input-init class="form-outline mb-4">
                                <label class="form-label" for="password">Password</label>
                                <input type="password" id="matkhau" class="form-control form-control-lg" name="matkhau"
                                    placeholder="Enter Password" required />
                            </div>

                            <div class="d-flex justify-content-center">
                                <button type="submit" data-mdb-button-init data-mdb-ripple-init
                                    class="btn btn-success btn-lg btn-block">Register</button>
                            </div>
                        </form>

                        <div class="text-center mt-3">
                            <p>Already have an account? <a href="/login">Sign in</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <footer class="text-center mt-auto">
            © 2025 AI ShortVideo. All rights reserved.
        </footer>
    </div>

    <!-- JavaScript for password toggle (optional, not used here but kept for consistency) -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // No password toggle needed for this form, but keeping the structure for consistency
        });
    </script>
</body>

</html>